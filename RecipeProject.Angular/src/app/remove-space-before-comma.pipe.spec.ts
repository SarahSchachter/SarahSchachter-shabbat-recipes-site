import { RemoveSpaceBeforeCommaPipe } from './remove-space-before-comma.pipe';

describe('RemoveSpaceBeforeCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSpaceBeforeCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
